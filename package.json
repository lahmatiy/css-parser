{
  "name": "css-parser",
  "version": "0.0.0",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha test/*.js",
    "coverage": "istanbul cover _mocha test/*.js",
    "codestyle": "jscs lib/ test/",
    "trace": "node --trace-hydrogen --trace-phase=Z --trace-deopt --code-comments --hydrogen-track-positions --redirect-code-traces --redirect-code-traces-to=code.asm --print-opt-code test"
  },
  "devDependencies": {
    "chai": "^3.4.0",
    "istanbul": "^0.4.0",
    "jscs": "^2.4.0",
    "mocha": "^2.3.3"
  }
}
